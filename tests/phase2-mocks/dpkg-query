#!/usr/bin/env bash

set -eu

package="${*: -1}"
if [[ "${NOVA_PHASE2_TEST_MISSING_PACKAGE:-}" == "$package" ]]; then
  exit 1
fi
if grep -Fqx -- "$package" "$NOVA_PHASE2_TEST_PACKAGE_STATE" 2>/dev/null; then
  printf 'ii '
  exit 0
fi
exit 1
